# 001: スライムキャラは 3D (Three.js) で実装する

## 決定

スライムのアニメーションは Three.js による 3D で実装する。

## 理由

- 伸縮（squash & stretch）が `scale` 操作だけで物理っぽく見える
- 2D (SVG/Canvas) だと「ぷにっと感」を出すためにパスアニメーションが必要になり手間が増える
- 目と口はテクスチャか geometry を表面に貼るだけで済む

## スコープ（今回）

- 体の伸縮
- 目と口の動き

## スコープ外（将来）

- 手・ステッキなどのパーツ追加
